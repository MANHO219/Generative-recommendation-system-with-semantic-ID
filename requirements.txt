# ===================================
# 服务器部署依赖 (Python 3.10.x)
# ===================================

# PyTorch (推荐通过 conda 安装，如已安装请注释掉)
# conda install pytorch==2.2.0 torchvision==0.17.0 pytorch-cuda=11.8 -c pytorch -c nvidia
torch>=2.2.0
torchvision>=0.17.0

# Transformers 生态
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
datasets>=2.18.0
accelerate>=0.29.0

# 量化库 (仅 Linux，Windows 不支持)
bitsandbytes>=0.43.0

# 数据处理
numpy>=1.26.0
pandas>=2.2.0
tqdm>=4.66.0

# 可视化
matplotlib>=3.8.0
tensorboard>=2.16.0

# 空间编码
openlocationcode>=1.0.1

# 工具库
einops>=0.8.0

# 模型下载（使用 ModelScope 时需要）
modelscope>=1.20.0

# 可选: 实验跟踪
# wandb>=0.19.0
